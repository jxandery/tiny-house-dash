<h1>Categories Page</h1>
<% @categories.each do |category| %>
  <%= link_to category.name, category_path(id: category.id) %></br>
<% end %>

<%= form_for Category.new do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.submit "Create" %>
<% end %>
